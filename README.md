# Pure CSS Page

A minimalist device mockup interface built entirely with HTML and CSS—no JavaScript required.

## Features

- **Pure CSS Design**: Built using only HTML and CSS
- **Device Frame**: Rounded rectangular frame with subtle border styling
- **Dynamic Island/Notch**: CSS-based notch cutout effect at the top center
- **Menu Navigation**: Clean menu items with active state styling
- **Responsive Layout**: Centered design that adapts to viewport size

## Getting Started

Simply open `index.html` in your web browser. No build process or dependencies required.

```bash
open index.html
